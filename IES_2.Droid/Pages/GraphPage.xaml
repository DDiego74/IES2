<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="IES_2.Droid.GraphPage"
             Title="Grafico Real-time"
             BackgroundColor="White">

    <Grid RowDefinitions="*,Auto">

        <lvc:CartesianChart Grid.Row="0"
            Series="{Binding ChartSeries}"
            XAxes="{Binding XAxes}"
            YAxes="{Binding YAxes}" />

        <Grid Grid.Row="1" ColumnDefinitions="Auto,*" Padding="12,8">
            <Label Grid.Column="0" Text="Parametro:" VerticalOptions="Center" Margin="0,0,8,0"/>
            <Picker Grid.Column="1"
                    ItemsSource="{Binding ParameterNames}"
                    SelectedItem="{Binding SelectedParameterName}"
                    VerticalOptions="Center"/>
        </Grid>

    </Grid>
</ContentPage>
